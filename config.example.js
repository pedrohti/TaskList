// Gerado automaticamente pelo GitHub Actions no deploy — não editar, não commitar com valores reais.
// Rodando local sem Actions: copia esse arquivo pra config.js e preenche na mão.
window.APP_CONFIG = {
  BIN_ID: '__BIN_ID__',
  API_KEY: '__API_KEY__'
};
